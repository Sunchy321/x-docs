%!TEX root = x.tex

\rSec0[core.order]{顺序库}

\pnum
\tcode{core.order}对各种序关系进行了定义。

\rSec1[core.order.type]{类型}

\begin{codeblock}
enum Order {
    less,
    equal,
    greater,
    unordered
}
\end{codeblock}